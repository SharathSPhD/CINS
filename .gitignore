.venv/
__pycache__/
*.pyc
.DS_Store
.pytest_cache/
.ruff_cache/
*.egg-info/
site/_site/
site/.jekyll-cache/
app/frontend/node_modules/
app/frontend/.next/
.remember/
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg

# Demo video intermediates: ~790 MB of per-frame PNGs and the padded canvases
# ffmpeg reads. Regenerable with experiments/make_demo_frames.py and
# make_demo_video.py; the encoded deliverables in site/assets/demo/ are tracked.
experiments/results/demo/frames/
experiments/results/demo/frames_vertical/
experiments/results/demo/wide/
experiments/results/demo/vertical/

# Development scaffolding and internal engineering notes. Kept locally, not
# published: agent configuration, TRIZ session logs, the per-directory
# instruction files, and the internal docs tree (spec, PRD, ADRs, gate-closure
# reports, design dossier). The public evidence base is experiments/results/,
# which remains tracked, and the manuscript cites that rather than these.
/.claude/
/.triz/
/docs/
CLAUDE.md
**/CLAUDE.md
paper/*.fls
paper/*.fdb_latexmk
paper/*.bbl
