# T8 ablation cell: alpha_free=true: angle of attack floats in the Newton system (opens the camber-alpha equivalence family; dossier/t7-winning-configuration memory item 5 predicts this converges to a DIFFERENT root than A*, which is the point of the ablation). n=8 only.
# One factor away from t8_n08_baseline.yaml unless noted.
cst:
  n_upper: 8
  n_lower: 8
  le_treatment: prescribed

transition:
  mode: forced

t8:
  airfoil: "2412"
  station_selection: qr_pivot
  init: presolve
  alpha_free: true
  dof_offset: 0
